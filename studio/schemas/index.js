// Schema registry — imported by sanity.config.js. Add new document
// types here when you add them.

import galleryImage from "./galleryImage";
import property from "./property";

export const schemaTypes = [galleryImage, property];
